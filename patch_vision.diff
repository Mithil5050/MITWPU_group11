--- /Users/mithil/Desktop/MITWPU_group11/Group_11_Revisio/Home/Games/DiagramGameViewController.swift
+++ /Users/mithil/Desktop/MITWPU_group11/Group_11_Revisio/Home/Games/DiagramGameViewController.swift
@@ -1,5 +1,6 @@
 import UIKit
+import Vision
 
 class DiagramGameViewController: UIViewController {
     
@@ -34,16 +35,6 @@
         selectedWord = nil
         hiddenWords.removeAll()
         
-        let allAnswers = [
-            "Golgi Apparatus", "Cell Membrane", "Cell Wall", "Ribosomes", "Nucleus", "Endoplasmic Reticulum",
-            "Golgi Vesicles", "Mitochondria", "Chloroplast", "Nucleolus", "Vacuole", "Cytoplasm"
-        ]
-        words = allAnswers.shuffled()
-        
-        // 2. Setup the Canvas Target Boxes
-        setupTargetBoxes()
-        
         // Collection View Setup
         wordBankCollectionView?.delegate = self
         wordBankCollectionView?.dataSource = self
@@ -53,24 +44,48 @@
             layout.scrollDirection = .horizontal
             layout.estimatedItemSize = UICollectionViewFlowLayout.automaticSize
             layout.minimumInteritemSpacing = 12
             layout.minimumLineSpacing = 12
         }
-        wordBankCollectionView?.reloadData()
+        
+        // Run Apple Vision to extract labels from the diagram image
+        extractLabelsFromDiagram()
     }
     
-    private func setupTargetBoxes() {
-        // Approximate relative positions (rx, ry) for the 12 labels
-        let positions: [(rx: CGFloat, ry: CGFloat, answer: String)] = [
-            // Left column
-            (rx: 0.16, ry: 0.38, answer: "Golgi Apparatus"),
-            (rx: 0.16, ry: 0.43, answer: "Cell Membrane"),
-            (rx: 0.16, ry: 0.50, answer: "Cell Wall"),
-            (rx: 0.16, ry: 0.58, answer: "Ribosomes"),
-            (rx: 0.16, ry: 0.65, answer: "Nucleus"),
-            (rx: 0.16, ry: 0.74, answer: "Endoplasmic Reticulum"),
+    private func extractLabelsFromDiagram() {
+        guard let image = diagramImageView.image, let cgImage = image.cgImage else { return }
+        
+        let handler = VNImageRequestHandler(cgImage: cgImage, options: [:])
+        let request = VNRecognizeTextRequest { [weak self] request, error in
+            guard let observations = request.results as? [VNRecognizedTextObservation] else { return }
             
-            // Right column
-            (rx: 0.84, ry: 0.35, answer: "Golgi Vesicles"),
-            (rx: 0.84, ry: 0.44, answer: "Mitochondria"),
-            (rx: 0.84, ry: 0.50, answer: "Chloroplast"),
-            (rx: 0.84, ry: 0.59, answer: "Nucleolus"),
-            (rx: 0.84, ry: 0.70, answer: "Vacuole"),
-            (rx: 0.84, ry: 0.81, answer: "Cytoplasm")
-        ]
+            DispatchQueue.main.async {
+                self?.processTextObservations(observations, imageSize: image.size)
+            }
+        }
         
-        for pos in positions {
-            let box = TargetBoxView(correctAnswer: pos.answer)
+        request.recognitionLevel = .accurate
+        
+        DispatchQueue.global(qos: .userInitiated).async {
+            do {
+                try handler.perform([request])
+            } catch {
+                print("Failed to perform text recognition: \(error)")
+            }
+        }
+    }
+    
+    private func processTextObservations(_ observations: [VNRecognizedTextObservation], imageSize: CGSize) {
+        var foundWords: [String] = []
+        
+        for observation in observations {
+            guard let topCandidate = observation.topCandidates(1).first else { continue }
+            let text = topCandidate.string.trimmingCharacters(in: .whitespacesAndNewlines)
+            
+            // Filter out the main title or stray small characters (could hook up LLM here for smarter filtering)
+            if text.lowercased().contains("structure") || text.lowercased().contains("anatomy") || text.count < 3 {
+                continue
+            }
+            
+            foundWords.append(text)
+            let box = TargetBoxView(correctAnswer: text)
             box.translatesAutoresizingMaskIntoConstraints = false
             diagramImageView.addSubview(box)
@@ -79,12 +94,21 @@
             let tap = UITapGestureRecognizer(target: self, action: #selector(didTapTargetBox(_:)))
             box.addGestureRecognizer(tap)
             
-            // Constrain using layout multipliers for proportional positioning
-            // We use NSLayoutConstraint to set proportional Center X and Center Y relative to the image view
+            // Vision Bounding Box coordinate system has origin at bottom-left. 
+            // `boundingBox` is normalized (0.0 to 1.0)
+            let bbox = observation.boundingBox
+            let safeCenterXMulti = max(CGFloat((bbox.minX + bbox.width / 2.0) * 2.0), 0.001)
+            let safeCenterYMulti = max(CGFloat((1.0 - bbox.midY) * 2.0), 0.001)
+            
             NSLayoutConstraint.activate([
-                NSLayoutConstraint(item: box, attribute: .centerX, relatedBy: .equal, toItem: diagramImageView, attribute: .centerX, multiplier: pos.rx * 2, constant: 0),
-                NSLayoutConstraint(item: box, attribute: .centerY, relatedBy: .equal, toItem: diagramImageView, attribute: .centerY, multiplier: pos.ry * 2, constant: 0),
-                box.widthAnchor.constraint(equalToConstant: 130),
-                box.heightAnchor.constraint(equalToConstant: 36)
+                NSLayoutConstraint(item: box, attribute: .width, relatedBy: .equal, toItem: diagramImageView, attribute: .width, multiplier: bbox.width * 1.5, constant: 32),
+                NSLayoutConstraint(item: box, attribute: .height, relatedBy: .equal, toItem: diagramImageView, attribute: .height, multiplier: bbox.height * 1.5, constant: 16),
+                NSLayoutConstraint(item: box, attribute: .centerX, relatedBy: .equal, toItem: diagramImageView, attribute: .centerX, multiplier: safeCenterXMulti, constant: 0),
+                NSLayoutConstraint(item: box, attribute: .centerY, relatedBy: .equal, toItem: diagramImageView, attribute: .centerY, multiplier: safeCenterYMulti, constant: 0)
             ])
         }
+        
+        words = foundWords.shuffled()
+        score = 0
+        wordBankCollectionView?.reloadData()
     }
